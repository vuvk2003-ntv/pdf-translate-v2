babeldoc==0.2.33
fonttools==4.63.0
numpy==2.2.6
pdfminer-six==20250416
# pdfminer pulls cryptography in and imports it while opening any document, so
# a broken build cannot even reach the first page. cryptography 49.0.0 dropped
# the macOS universal2 wheel: an Intel build then compiles it against Homebrew's
# OpenSSL while PyInstaller bundles Python's older libssl.3.dylib, and the
# packaged app dies on "Symbol not found: _SSL_get0_group_name". 48.0.1 is the
# last release whose wheel still covers Intel. Other platforms resolve it
# transitively, as before.
cryptography==48.0.1; sys_platform == "darwin" and platform_machine == "x86_64"
pikepdf==10.12.0
pymupdf==1.25.2
requests==2.34.2
tenacity==9.1.4
tqdm==4.70.0

# Imported directly by the bundled core but previously reached only through
# babeldoc, so nothing pinned them. Not the headless opencv build: babeldoc
# requires the full package, and installing both just shadows one with the other.
# onnx is the exception: nothing here imports it any more - doclayout reads the
# model metadata off the onnxruntime session instead - but babeldoc still pulls
# it in, and every release through 1.21.0 carries a path-traversal or
# malicious-model advisory. Pinned to hold that transitive dependency at the
# first version clear of all of them.
onnx==1.22.0
onnxruntime==1.20.1
opencv-python==5.0.0.93
peewee==3.17.9
